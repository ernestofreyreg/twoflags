{
  "title": "segment",
  "type": "object",
  "x-examples": {
    "example-1": {
      "id": "experiment002",
      "type": "weighted_choice",
      "params": {
        "options": [
          "A",
          "B"
        ],
        "weights": [
          0.2,
          0.8
        ]
      },
      "name": "Experiment 002: New Sales Page",
      "description": "New Sales Pages converts better"
    }
  },
  "description": "A Segment describes a way to segment a Flag according to client identity (user id or some type of correlation id) We use weighted choices.",
  "properties": {
    "id": {
      "type": "string"
    },
    "type": {
      "type": "string"
    },
    "params": {
      "type": "object",
      "properties": {
        "options": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "weights": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    }
  }
}